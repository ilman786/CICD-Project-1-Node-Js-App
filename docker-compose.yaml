version: '3.8'
services:
  app:
    image: ${IMAGE_NAME:-my-nodejs-app}:latest
    container_name: my-nodejs-app
    ports:
      - "${HOST_PORT:-80}:8000"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=8000
